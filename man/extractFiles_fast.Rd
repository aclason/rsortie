% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OutputFunctions.R
\name{extractFiles_fast}
\alias{extractFiles_fast}
\title{Trial extract fast}
\usage{
extractFiles_fast(
  itype = 1,
  exname,
  tarnames = NULL,
  extract_subdir = "extracted",
  keep_gz = TRUE
)
}
\arguments{
\item{itype}{\link{integer}
Extraction mode.
\describe{
\item{0}{Extract all \code{.tar} files found in \code{exname}.}
\item{1}{Extract only the \code{.tar} files specified in \code{tarnames}.}
}}

\item{exname}{\link{character}
Path to the directory containing the \code{.tar} files to be extracted. The
extracted files will be written to a subdirectory within this path.}

\item{tarnames}{\link{character or NULL}
Character vector of \code{.tar} file names (not full paths) to extract when
\code{itype = 1}. Ignored when \code{itype = 0}.}

\item{extract_subdir}{\link{character}
Name of the subdirectory (within \code{exname}) where extracted files will be
placed. Defaults to \code{"extracted"}.}

\item{keep_gz}{\link{logical}
Should the original \code{.gz} files be retained after decompression?
\describe{
\item{TRUE}{Keep the \code{.gz} files (default).}
\item{FALSE}{Remove \code{.gz} files after successful decompression.}
}}
}
\value{
Invisibly returns a character vector of paths to the extracted (and
uncompressed) \code{.xml} files. Returns \code{NULL} if no matching files are found.
}
\description{
Efficiently extracts files from \code{.tar} archives and optionally uncompresses
\code{.xml.gz} outputs. Designed to minimize disk I/O and improve performance,
especially for large batch extraction and parallel workflows.
}
